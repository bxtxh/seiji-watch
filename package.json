{
  "name": "seiji-watch",
  "version": "0.1.0",
  "description": "Diet Issue Tracker - Independent platform tracking Diet issues as tickets",
  "private": true,
  "workspaces": [
    "services/web-frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=services/web-frontend",
    "build": "npm run build --workspace=services/web-frontend",
    "start": "npm run start --workspace=services/web-frontend",
    "lint": "npm run lint --workspace=services/web-frontend",
    "test": "npm run test --workspace=services/web-frontend"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}